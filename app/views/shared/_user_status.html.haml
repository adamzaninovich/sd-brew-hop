- if current_user
  %p.navbar-text.navbar-right
    Logged in as #{current_user.name}
    (#{link_to 'Sign out', sign_out_path, class: 'navbar-link'})
- else
  = button_to 'Sign in with Facebook', sign_in_with_facebook_path, class: "btn btn-default navbar-btn navbar-right"

